# 项目进度

## 总计划

- [x] P0. 基线：4-Agent 系统（Vox/Lynx/Fixer/Judge）+ 后台看板 + 防御性设计
- [x] P1. Phase 1：基础设施加固（Tool Gateway + 记忆系统 + Git 沙盒）
- [ ] P2. Phase 2：编排能力提升（异步编排 + 断点续传）
- [x] P3. Phase 3（部分）：Smith 元Agent + 数据分析闭环
- [ ] P3. 待做：成本优化（LLM 缓存 + 模型路由）

---

## 当前状态：P1 完成 + M7 完成，剩余 P2（M4/M5）+ M6

**最新提交：** `8707ca8 feat: M7-b Smith数据分析闭环`

### Agent 体系

| 角色 | 模式 | 职责 | 工具权限（由 M1 Gateway 保障） |
|------|------|------|-------------------------------|
| **Vox** | primary | 总指挥：7 步工作流调度 | 只有 task |
| **Lynx** | subagent | 侦察兵：搜代码/查文档/调研 | read, grep, glob, context7*, gh_grep, webfetch |
| **Fixer** | subagent | 双手：改代码/跑命令/验证 | read, write, edit, bash (+ 危险命令检测) |
| **Judge** | subagent | 显微镜：审计合规/测试/日志 | read, grep, glob |
| **Smith** | subagent | 锐匠：每 30 轮分析数据/提优化建议 | read, grep, glob |

### Hooks 注册清单（src/index.ts）

| Hook | 功能 | 来源 |
|------|------|------|
| `tool.execute.before` | 工具权限校验 + 看板任务注册 | gateway + taskManager |
| `tool.execute.after` | 看板状态更新 + Judge/Fixer 事件采集 | taskManager + analytics |
| `experimental.chat.messages.transform` | 用户消息末尾注入后台任务看板 | taskManager |
| `experimental.chat.system.transform` | 系统约束注入 + 长期记忆注入 + Smith 激活 | systemTransform + memory + smithTrigger |
| `experimental.session.compacting` | 会话压缩时提取 lessons 写入 .xagt/memory.jsonl | memory/rollover |
| `chat.message` | Vox 输出拦截（含代码无 task()）+ Smith 频率计数 | — |
| `chat.params` | 按 Agent 注入差异化 thinking/reasoning_effort | config |
| `event` | session idle 时清理已 reconcile 的看板任务 | taskManager |
| `config` | 注册 5 个 Agent + 合并用户配置 | — |

### 已完成

| 模块 | 提交 |
|------|------|
| M1 Tool Gateway — 声明式权限策略 | `42c25dd` |
| M2 持久化记忆 — JSONL 存储引擎 + 自动轮转 | `abd393b` |
| M3 Git 沙盒 — Worktree 物理隔离 | `abd393b` |
| M7-a Smith 元Agent — 30轮激活触发器 | `abd393b` |
| M7-b Smith 数据分析闭环 — Collector + 分析报告 | `8707ca8` |
| [技术债] 记忆 hooks 挂载 — session.compacting + system.transform 注入 | `(待提交)` |
| [技术债] Vox 派活三段式 — Fixer/Lynx/Judge 模板化 | `(待提交)` |
| M1 补充：resolveAgentFromSession 工具函数 | `42c25dd` |

### 待做

1. M6 成本优化 — LLM 响应缓存 + 模型路由
2. M4 异步编排 — 并行/条件编排
3. M5 断点续传 — State Restoration
